{
  "_comment": "Project settings (committed). PreToolUse: ops/guard_edit.py makes the KA7 safe-write fence MECHANICAL (denies Edit/Write/NotebookEdit on the armed live-trader path + secrets + raw DBs from context_map.json edit_policy; fail-open). SessionStart: ops/session_orient.py injects the context-map orientation (ctx brief + a navigate-via-ctx directive) so a new chat queries instead of bulk-reading; fail-safe. See ops/hooks/ for the matching pre-commit secret-scan.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/venv/bin/python \"$CLAUDE_PROJECT_DIR\"/ops/guard_edit.py"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/venv/bin/python \"$CLAUDE_PROJECT_DIR\"/ops/session_orient.py",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
